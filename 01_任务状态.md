# 01 任务状态清单：瑞芬太尼 OIH 信号 FAERS 头对头

> 文件夹：`瑞芬太尼/`（remifentanil；套路：FAERS 单药物 SOC 分类安全性评估 ｜ 方案 6-2）
> 复用：同目录**氯胺酮管线**（已 Frontiers 定稿）作脚本骨架与经验库
> 最近更新：2026-09-16（**I 步英文 IMRaD 正文初稿已完成**，全部数字经 `_check_consistency.py` 程序化自检 **94/94 PASS**；修掉 `cv_soc_27.csv` 表头列序错位 bug；openFDA 原生 27 SOC 面板免 key 跑通）

## ⚠️ 路线校准（环境实测，写入即诚信）

**已实测的主机可达性（2026-09-16）：**
| 主机 | 状态 | 含义 |
|---|---|---|
| `api.fda.gov`（openFDA 聚合层） | ✅ 可达 | `search+total` 端点**免 key 可用**；`count`（字段聚合）端点**免 key 也可用，但仅限 `limit ≤ 500`**（`limit=1000` → 403 `API_KEY_MISSING`）；实测矩阵见 §方法学备忘 9 |
| `fis.fda.gov`（FAERS ASCII 个案层） | ❌ **硬封** HTTP=000 | 个案层、drug-record 级 route、真实 TTO 时间戳**永久不可行** |
| `info.pmda.go.jp`（JADER） | ✅ 可达 HTTP=200 | JADER CSV/XML 公开免注册下载 → 可作第二验证库 |
| `open.canada.ca` → `opencanada.blob.core.windows.net`（Canada Vigilance） | ✅ 可达 HTTP=206 | ZIP 直链已拿；`Reactions.txt` **原生带 `SOC_NAME_ENG`/`PT_NAME_ENG`** |

**关键结论与对策：**
1. **核心分析（01/02）安全**：全程用 `search+total` 端点（免 key），已缓存且复测一致（REMI AND PAIN=23）。头对头 OIH 结论不受 key 影响。
2. **fis.fda.gov 硬封** → FAERS 个案层管线永久放弃；TTO/真实时间戳、drug-record 级精确 route **不可做**。
3. **openFDA `count` 端点免 key 可用（已修正——早期"需 API key"系误判）**：真凶是 `limit=1000`；改 **`limit=500`** 即完全免 key（四药映射覆盖率 93.7–96.9%，全局 98.8%）。→ D 步 27 SOC 现为**双库并列**：openFDA 事件级近似（`03_soc_27.csv`）+ Canada 报告级原生（`cv/cv_soc_27.csv`，权威）。
4. **双库设计升级**：FAERS（openFDA 聚合层）= 主分析（头对头 OIH 失衡）；Canada Vigilance = 独立验证库（原生 SOC 全景 + 头对头信号复现 + 亚组）。必要时再加 JADER 作第三库。这比原"单库"更强、更可发表。
5. **openFDA API key（可选，已不再必需）**：`count` 面板已免 key 产出。若仍需 key（要 top-1000、或日调用超 1,000 次/IP）：`https://api.data.gov/signup/`。
   ⚠️ 实测**国内邮箱（QQ）收不到 key 邮件**（服务端拒收，垃圾箱/广告箱均无）→ 改用 Gmail/Outlook/学校邮箱或临时邮箱；兜底邮件索要 `open@fda.hhs.gov`。详见 `X_openFDA_key收不到邮件_排查与替代方案.md`。

## 总体进度

| 模块 | 状态 | 数据 / 可行性 |
|---|---|---|
| 数据获取（聚合层 openFDA） | ✅ 已完成 | N=20,692,687；瑞芬 5,375 / 芬太尼 121,819 / 舒芬 6,513 / 吗啡 56,501（均 search+total，免 key） |
| 核心四算法 + 头对头 RORR（OIH + 阴性对照） | ✅ 已完成 | 真实计数；ROR/PRR/IC(BCPNN)/EBGM 四算法 + RORR（01/01_summary） |
| C1 给药途径分层（探索性敏感性） | ✅ 已完成 | openFDA 聚合层；nested 局限声明；强化混杂假说 |
| D 27 SOC 全景 | ✅ 已完成（**双库并列**） | ① openFDA 原生（**免 key，limit=500**，事件级近似）`03_soc_27.csv`；② Canada 原生报告级（权威）`cv/cv_soc_27.csv`。双库均：无任何 SOC 指向痛觉敏化；含 PT "PAIN" 的 General disorders 瑞芬 ROR 0.188（RORR 0.141/0.058）与 PT 层/Canada 层同向 |
| E 报告年份趋势（TTO 降级替代） | ⏸ 待定 | Canada/FAERS 均无 drug-start 日期；仅能按"报告年份"做趋势（呼应 Weber 效应），优先级降 |
| F 亚组（sex/age/reporter/serious） | ✅ 已完成 | Canada 原生字段；**报告者列定位修正（col34，原 col39 误抓厂商号）**；瑞芬严重报告 91.9% vs 吗啡 69.0% |
| G 跨库验证 | ✅ 已完成 | FAERS 主分析 vs Canada 验证库；**OIH 特定 PT 双库皆 0、PAIN 低报方向双库复现** → 独立复现阴性结果 |
| H 敏感性（PS-only / 年份分层） | ✅ 已完成 | **仅严重报告子集**（N_ser=11.88M，瑞芬 98% 严重）: OIH PT 仍全 0、PAIN RORR vs 芬 0.072；**年份分层**（PAIN 2015–2024）: 每年 RORR<1，方向无反转 |
| 三线表图 / 正文 / 去 AI / 投稿包 | 🔄 **正文初稿已完成** | `I_稿件三线表.md`（Table 1–4）+ Fig 1–2 + **`I_正文_IMRaD_en.md`（Abstract/Intro/Methods/Results/Discussion/Limitations/Conclusion + 18 条已核实文献 + 待补缺口清单）**；剩 J 去 AI 润色 + 质量门禁 + 投稿包组装 |

## 详细步骤状态（依方案二技术路线，已按实测校准）

| # | 步骤 | 状态 | 说明 |
|---|---|---|---|
| 1 | 数据获取与队列构建 | 🔶 主库(openFDA)✅ / 个案层❌(硬封) / 验证库(Canada)⬇️下载中 | openFDA 全历史 any-role 分母；Canada Vigilance 独立验证库 |
| 2 | 构建队列（瑞芬/芬/舒/吗） | ✅ | activesubstance.exact 单字段（含主要盐型），与 01/02 对齐 |
| 3 | 四算法失衡（ROR/PRR/IC/EBGM） | ✅ | 13 PT 词典（01） |
| 4 | 头对头 RORR | ✅ | 核心创新；瑞芬全面低报 |
| 5 | 给药途径分层 | ✅(探索性) | C1；nested 局限声明 |
| 6 | SOC 全景 | ✅ | **双库并列**：openFDA 原生（免 key，limit=500，事件级近似，见 `D_27SOC_openFDA事件级.md`）+ **Canada 原生报告级（权威）**；瑞芬画像=强效 IV 阿片（免疫/心血管/呼吸偏高），无 OIH 指向 |
| 7 | TTO + Weibull | 🔻 降级/取消 | 无 drug-start 日期；改"报告年份趋势"或省略 |
| 8 | 亚组（性别/年龄/角色/严重度） | ✅ | **Canada 原生字段**（age/gender/reporter/seriousness）；报告者列定位修正 col34 |
| 9 | 严重结局分析 | ✅ | Canada `seriousness` 字段：瑞芬严重 91.9% vs 吗啡 69.0%（围术期语境） |
| 10 | 跨库验证 | ✅ | FAERS 主分析（n=5,375 瑞芬） vs Canada 验证库（n=111 瑞芬）；OIH PT 双库皆 0，PAIN 低报方向双库复现 |
| 11 | 敏感性分析 | ✅ | 仅严重报告（`serious:1`）+ PAIN 年份分层；OIH 缺失与 PAIN 低报方向均稳健 |

## 本轮交付物（已完成）

- `00_项目总览与执行路线图.md`：理清 + 7 处改进 + 路线图 + 质量门禁
- `01_核心FAERS失衡分析.py` + `01_faers_results.csv` + `01_faers_summary.md`：核心四算法 + 头对头 RORR（search+total，免 key）
- `02_途径分层分析.py` + `02_route_stratified.csv` + `02_route_summary.md`：C1 探索性途径分层
- `03_soc_aggregate_openfda.py` + `03_soc_27.csv` + `03_soc_run.log`：**openFDA 原生 27 SOC 全景（免 key，已跑通，5 次调用 / 1 min 24 s）**；事件级近似，作 Canada 报告级表的定性互证
- `D_27SOC_openFDA事件级.md`：D 步方法/结果/局限/溯源全记录（含免疫信号 PT 级拆解）
- `01_任务状态.md`：账本（本轮修正外数据源实测结论）
- `_faers_cache.json`：openFDA 查询缓存（search+total 结果完好）
- `cv/extract_extrait.zip` + `cv/cvponline_extract_20241130/`：Canada Vigilance 数据（**已下载解压**，覆盖至 2021-12-31，报告 1,154,017 份，原生 SOC）
- `cv/cv_process.py` + `cv/cv_soc_27.csv` + `cv/cv_pt_summary.csv` + `cv/cv_subgroups.csv` + `cv/cv_drug_totals.csv` + `cv/cv_summary.md`：Canada 验证库全流程（报告者列 col34 已修正）
- `G_跨库验证_FAERSvsCanada.md`：FAERS 主分析 vs Canada 验证库 头对头并列表 + 复现结论 + 分歧解读
- `04_sensitivity.py` + `04_sensitivity_ps_only.csv` + `04_sensitivity_year_pain.csv` + `04_sensitivity_summary.md`：H 步敏感性（仅严重报告子集 + PAIN 年份分层）
- `05_figures.py` + `I_fig1_rorr_forest.png/.svg` + `I_fig2_year_trend.png/.svg`：稿件图 1–2（RORR 森林图、PAIN 年份趋势）
- `I_稿件三线表.md`：投稿三线表 Table 1–4 + 图注 + 溯源对照表
- `I_正文_IMRaD_en.md`：**英文 IMRaD 正文初稿**（结构化 Abstract + Introduction + Methods 八节 + Results 九节 + Discussion 七节 + Conclusion + Declarations + 18 条参考文献（DOI/PMID 已核实）+ §9 数字溯源表 + §10 待补缺口清单）
- `_check_consistency.py`：**正文数字一致性自检脚本**（94 项断言，全部 PASS；可重复运行作质量门禁）
- `_fda_auth.py` + `.gitignore` + `X_openFDA_key使用说明.md`：openFDA API key 环境变量读取（脚本已 key-ready）
- `X_openFDA_key收不到邮件_排查与替代方案.md`：**key 邮件收不到**的根因判断 / 换邮箱与临时邮箱方案 / QQ 邮箱自助查询 / 向 `open@fda.hhs.gov` 索要的英文邮件模板

## 关键科学发现（累积，写入即诚信）

1. **OIH 相关 PT 在 FAERS 结构性缺失**：HYPERALGESIA / PAIN INCREASED / POSTOPERATIVE PAIN / CHRONIC PAIN / OPIOID WITHDRAWAL 全库计数 0（独立 404 复核）。
2. **唯一可分析 OIH 邻域 PT = ALLODYNIA**：瑞芬 a=1（无信号），芬太尼 ROR 7.64 / 吗啡 10.15（强信号）——方向与 RIH 相反。
3. **瑞芬全面低报**：PAIN ROR 0.14、阴性对照（恶心 0.245/呕吐 0.527/瘙痒 0.419/便秘 0.197）均 <1；头对头 RORR 全部显著 <1。
4. **C1 强化混杂假说**：芬太尼透皮 PAIN ROR 3.38 > IV 1.67；适应症梯度 瑞芬 0.13 < 芬太尼 1.67 < 吗啡 3.42；同途径 RORR 仍极低 → 低报=报告构成/适应症混杂，非保护效应。
5. **诚实解读**：FAERS 无证据支持 RIH，且结构性不适合检测 OIH。Canada Vigilance **已独立验证**此结论。
6. **跨库验证结论（G 步）**：OIH 特定 PT（HYPERALGESIA/PAIN INCREASED/POSTOPERATIVE PAIN/CHRONIC PAIN/OPIOID WITHDRAWAL）在 FAERS 与 Canada **双库计数均为 0**；PAIN 低报方向双库复现（RORR 瑞芬 vs 芬 0.066→0.235、vs 吗 0.046→0.146，均 <1）；ALLODYNIA 瑞芬双库均无信号而芬/吗有信号。论文升级为"双主权库独立复现阴性结果"。
7. **亚组佐证（F 步）**：Canada 瑞芬队列严重报告占比 91.9% vs 吗啡 69.0%，与其短效 IV 围术期阿片场景一致——"疼痛 PT 低报"的结构性成因为报告构成/适应症混杂，非镇痛保护。DRUG INEFFECTIVE 在 Canada 反呈高报（RORR 1.277/1.703，医师-only 5.921/10.604），反向证明低报具有特异性而非全局偏倚。
8. **敏感性稳健（H 步）**：(a) 仅严重报告子集（N_ser=11,882,968；瑞芬 5,375 报告中 5,270=98.0% 为严重）中，OIH 特定 PT 仍全为 0，PAIN RORR vs 芬 0.072（与全库 0.066 几乎一致）→ 对报告严重度稳健；(b) PAIN 分年（2015–2024）RORR 瑞芬 vs 芬/吗 **每一年均<1**，方向无年度反转 → 非时间特异伪影。
9. **管线灵敏度正对照（D 步 openFDA SOC 面板，重要方法学收获）**：openFDA 27 SOC 面板检出多个**强信号** —— Immune system disorders ROR **10.95**（RORR vs 芬 8.61 / vs 吗 2.78）、Surgical procedures 7.68、Pregnancy 5.98、Cardiac 5.55。PT 级拆解显示免疫 SOC 的 81% 由 ANAPHYLACTIC SHOCK(532) + ANAPHYLACTIC REACTION(367) 驱动。
   **双重意义**：① **证明管线能检出真实存在的信号** → "OIH 阴性"更可能是真阴性，而非方法失灵；② **印证报告构成假说** —— 瑞芬 FAERS 报告池由急性围术期事件（过敏/低血压/心搏骤停/急性肾损伤）主导，而非"镇痛不足"类报告，这正是"PAIN 低报"的结构性成因，**不是瑞芬具镇痛保护作用**。
   ⚠️ 该非 OIH 信号**超出本文范围，不作因果解读**；但"免疫系统"在 Canada 库 SOC 画像中同样居瑞芬前列 → **双库同向**，值得作为独立课题研究。

## 论文走向（已决策）

> *No disproportionate real-world reporting of hyperalgesia with remifentanil versus other intraoperative opioids: a FAERS–Canada Vigilance signal study challenging remifentanil-induced hyperalgesia*

高价值证伪 / 阴性结果 + 双库验证，可投 Anaesthesia / BJA / J Clin Anesth / Regional Anesthesia & Pain Medicine / Therapeutic Advances in Drug Safety。

## 关键方法学备忘（写入即诚信）

1. disproportionality ≠ 风险：无暴露分母，仅"信号"非"因果"。
2. **nested 缺陷**：openFDA report-level search 使 route 被同 report 他药错配（瑞芬纯 IV 却 ~21% oral 为铁证）；C1 仅探索性。
3. **网络约束（实测）**：fis.fda.gov 硬封；JADER/Canada 可达；openFDA `count` 端点**免 key 可用（limit ≤ 500）**，仅 `limit=1000` 需 key。
4. **双库设计**：FAERS 主 + Canada Vigilance 验证（原生 SOC/年龄/性别/报告者/严重度）。
5. 药物计数口径：瑞芬 5,375（含 HYDROCHLORIDE）/ 芬太尼 121,819 / 舒芬 6,513（含 CITRATE）/ 吗啡 56,501。
6. 区域阻滞不可经 FAERS 追踪 → F2 合并镇痛药亚组仅含 NSAIDs/氯胺酮/右美托咪定（验证库侧同样处理）。
7. **openFDA `count` 端点与 API key（已修正）**：`count` 端点**免 key 可用**，唯一限制是 **`limit` 必须 ≤ 500**（`limit=1000` → 403 `API_KEY_MISSING`）。故 27 SOC 面板无需 key 即可产出；key 仅对"要 top-1000"或"日调用 > 1,000 次/IP"有意义。
8. **openFDA 字段陷阱（实测，写入即诚信）**：
   - **严重度字段在顶层**：`serious`（合并严重标志，"1"/"0"）与 `seriousnessdeath/seriousnesslifethreatening/seriousnesshospitalization/seriousnessdisabling/seriousnesscongenitalanomali/seriousnessother` 均在**记录顶层**，**不在 `patient.*` 下**；写 `patient.seriousnessdeath` 或 `serious.seriousnesshospitalization` 均返回 404 No matches。
   - **日期范围用字面空格**：`receivedate:[20150101 TO 20151231]`；若写成 `+`（如 `20150101+TO+20151231`），urlencode 会把 `+` 编码为 `%2B` 导致 **HTTP 500**。
   - **API key 读取**：脚本经 `_fda_auth.py` 从 `OPENFDA_API_KEY` 环境变量或 `openfda_key.txt` 读取，不硬编码。
9. **`count` 端点免 key 阈值矩阵（2026-09-16 实测，本环境，全程未用 key）**：

   | 请求 | 结果 |
   |---|---|
   | `count=<field>`（无 search），任意 limit | ✅ 200 |
   | `count=<field>&search=<任意>&limit=100 / 200 / 500` | ✅ 200 |
   | `count=<field>&search=<任意>&limit=1000` | ❌ **403 `API_KEY_MISSING`** |
   | `search=<任意>&limit=1`（total 端点） | ✅ 200 |

   → 免 key 跑 `count` **一律用 `limit=500`**。`API_KEY_MISSING` 必须当作**不可重试**错误（立刻中止或降级），
   不要混进 403/429 退避循环（8 次退避会白等约 10 min/条）。已同步修正 skill `openfda-disproportionality`。
10. **`soc_rules.py` 是启发式关键词映射，非权威 MedDRA**：如 PT "DRUG INEFFECTIVE" 被规则归入 Injury/poisoning/procedural，
    而权威 MedDRA 归入 General disorders / Product issues；KOUNIS SYNDROME、NEUROMUSCULAR BLOCK PROLONGED 等未映射。
    → openFDA SOC 表**仅作定性互证**；**报告级原生 SOC 权威表 = `cv/cv_soc_27.csv`（Canada）**。
11. **CSV 写出陷阱（本轮实际踩到，务必记住）**：
    - **交错列序必须与表头一致**：`cv_process.py` 写 SOC 表时，循环内每药写入的是 `(reports, pct)` **两个交错列**，
      而原表头却写成"四药 reports 全在前、四药 pct 全在后" → **列名与数据错位**（数据本身正确，标签错误）。
      已修脚本（`[f"{t}_{k}" for t in TARGETS for k in ("reports","pct")]`）并修补 `cv_soc_27.csv` 表头，
      全表 27 SOC × 4 药逐格校验 `pct == reports/队列` **0 处不一致**。
      → **教训**：写出交错数据的 CSV 后，必须做一次"标签-数值一致性"校验（如 pct 应等于 count/denominator）。
    - **长表加 `##` 注释行会破坏 `csv.DictReader`**：`03_soc_27.csv` 开头有 5 行说明，直接 DictReader 会把注释当表头。
      读取时须先定位真正表头行（`next(i for i,ln in enumerate(lines) if ln.startswith("SOC,"))`）。
12. **正文质量门禁**：`_check_consistency.py` 对正文引用的 94 个数字做程序化断言（FAERS PT 层 / Canada PT 层 / Canada 原生 SOC / 亚组 / 敏感性 / openFDA SOC / 免疫 PT 拆解 / 年份 RORR 全 <1）。
      改数据或改正文后**必须重跑**；返回非 0 即不得视为可投稿。
